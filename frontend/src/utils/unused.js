// ** Merges two queries into one array and sorts all by updatedAt date/time
// function mergeDataSources(a, b) {
//   const newA = a.concat(b);
//   newA.sort((a, b) =>
//     a._updatedAt < b._updatedAt ? 1 : b._updatedAt < a._updatedAt ? -1 : 0
//   );
//   return newA;
// }
